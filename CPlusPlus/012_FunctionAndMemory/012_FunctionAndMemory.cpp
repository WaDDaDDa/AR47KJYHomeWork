
#include <iostream>


// 내가 나만의 실행흐름을 만들고
// 그 실행흐름에 이름을 붙일수 있다.
// 그것을 함수라고 합니다!
// 함수!!

void Functionn()
{
    {
        int Value = 20;
    }

    {
        int Value = 30;
    }

    // ? 동일한 변수를 선언은 불가능한데 어떻게 가능한가요?
    // 서로 다른 실행흐름에서의 변수 선언이기때문에 가능합니다.

    return;
}


// main scope
int main()
{
    // 함수의 시작
    // 실행흐름의 시작

    // 스코프
    // 실행흐름의 시작과 끝을 표현하는 기호. {}중괄호

    // 실행흐름을 언제든지 만들수 있다.
    // 

    // Unname scope
    // 이름이 없는 실행흐름
    {
        int Value = 100;
    }


}
